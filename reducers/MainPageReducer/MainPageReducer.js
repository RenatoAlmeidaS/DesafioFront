const INITIAL_STATE = {
photo: '../static/imgs/perfil.png',
name: 'Renato'
};

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            break;
    }
    return state;
};
